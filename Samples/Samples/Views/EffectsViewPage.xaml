<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin.Samples.Views.EffectsViewPage"
             xmlns:effects="clr-namespace:Xamarin.Forms.Toolkit.Effects;assembly=Xamarin.Forms.Toolkit"
             Title="View effects"
             BackgroundColor="DarkGray">
    <ScrollView Padding="10">
        <StackLayout Spacing="10">

            <Label Text="Blur a view containing an image" />

            <Slider x:Name="BlurSlider" Minimum="0" Maximum="25" />
            <Image BindingContext="{x:Reference BlurSlider}"
                   Source="{OnPlatform UWP='Assets/xamarin_logo.png', Default='xamarin_logo.png'}"
                   effects:ViewBlur.BlurAmount="{Binding Value}" />

            <Image Source="{OnPlatform UWP='Assets/xamarin_logo.png', Default='xamarin_logo.png'}"
               effects:ViewBlur.BlurAmount="10"/>

        </StackLayout>
    </ScrollView>
</ContentPage>